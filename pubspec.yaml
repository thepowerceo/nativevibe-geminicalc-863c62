name: gemini_calc
description: To provide users with a sophisticated scientific calculator app featuring a retr

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  provider: ^6.1.2
  go_router: ^14.2.7
  intl: ^0.19.0
  google_fonts: ^6.1.0
  math_expressions: ^2.4.0
  shared_preferences: ^2.2.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true